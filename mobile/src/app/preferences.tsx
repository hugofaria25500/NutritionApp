import { Text, View } from 'react-native';
import PreferencesScreen from '@/components/register/steps/PreferencesScreen';

export default function Goal() {
  return (
    <PreferencesScreen />
  );
}
